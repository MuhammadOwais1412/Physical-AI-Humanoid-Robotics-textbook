{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/((?!static|.*\\..*|_next|favicon\\.ico|manifest\\.json).*)",
      "destination": "/index.html"
    }
  ]
}